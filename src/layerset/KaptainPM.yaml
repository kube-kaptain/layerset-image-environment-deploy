apiVersion: kaptain.org/1.7
kind: docker-build-dockerfile
metadata:
  description: "Image environment deploy layerset - composes quality and docker build settings"

spec:
  layers:
    - layer-github-flow-strict:[1.2,2.0)
    - layer-image-environment-deploy:[1.1,2.0)
